# Advent of Code (AoC) - day 12, task 1

import "prelude.eg"

using System, OS, List, R = Regex

def main =
   read_lines stdin |> split_on "" |> last |> map (R::matches (R::compile "[0-9]+"))
   |> map (map to_int) |> filter [{W,H|NN} -> (8 * (sum NN)) <= (W*H)] |> length
